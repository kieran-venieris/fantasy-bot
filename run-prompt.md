Fantasy full-manager routine. Work in the repo root. FIRST: `cat CLAUDE.md` and follow it; it is your memory. Then `set -a && source .env && set +a`, `python3 league_state.py > /tmp/s.json && cp /tmp/s.json state.json`, read state.json (roster, matchup, free agents, recent_activity, pending_transactions). NEWS (rule 11): run `python3 search_targets.py` and web-search ONLY the players it prints — at most 10 searches per run, one per player, done yourself with WebSearch (no subagents, no WebFetch). No searches for healthy starters or anyone else; for them ESPN's injury tag and projections in state.json are the source. Steps, all with `python3 transact.py ... --live` and dry-run first when unsure: (1) LINEUP: optimal 9 slots on ESPN weekly projections via `lineup --live`; never bench ACTIVE for lower-projected; OUT/DOUBTFUL/IR to BE before lock; skip locked players. (2) INCOMING TRADES: `transact.py incoming`. For each proposal sent to my team ($TEAM_ID): `respond --id <id> --action accept --live` ONLY if Δ ≥ +15 ROS lineup points, roster keeps ≥5 RB and ≥5 WR, and any --drop needed for roster room is a bench player (the script enforces this; never --force). Otherwise `respond --action reject --live`, and if `incoming` printed a one-player-swap counter that clears the bar, propose it with `trade` (counts toward the weekly cap). After every live respond, record the ESPN response id AND the EXECUTED TRADE_ACCEPT/TRADE_DECLINE record id+status the script prints, in log.md and in the brief. (3) ADDS: `add --waiver` unless state.json shows free agent; Δ ROS lineup > 0 required (script-enforced), bench-only drops unless the replacement projects higher, max 2/run, no 3rd QB / 2nd K. (4) TRADES: max 3 proposals per NFL week including the owner's manual ones (grep log.md); before any trade run `transact.py pending` and read state.json pending_transactions, skip if a proposal already involves my team and the target; never withdraw a proposal the owner sent by hand; core players (Allen, Hall, A. Jones, D. Smith, J. Williams, Warren) only for Δ ≥ +15 and ≥5 RB/≥5 WR afterwards. State the Δ number for every move in the brief. Any non-200: log the response text, no retry of that action until the next calendar day. Append every executed transaction to log.md with timestamp and ESPN id. Write the brief to briefs/YYYY-MM-DD-HHMM.md and copy it to BRIEF.md; it must end with a "Decisions I made that you might disagree with" section. LAST: update CLAUDE.md (decision log, opponent notes) and commit locally (python3 -m py_compile transact.py search_targets.py first; never git push; never stage .env or state.json).
